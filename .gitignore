/build/
/.gradle/
/target/
.classpath
.project
/.settings
zugang.txt
temp.sqlite
memory
.factorypath
/.apt_generated/
/.apt_generated_tests/
src/test/resources/smokeTest.sqlite

.claude/settings.local.json
CLAUDE.local.md
/.claude/worktrees/
/.claude/cache/
/.claude/.tmp/
# Persisted Claude login + history (devcontainer bind mount) — folder is committed empty; its contents (live tokens) never are
/.devcontainer/.claude-home/*
/logs.txt
/database/

# Secrets, keystores, and machine-specific helper scripts (never commit — CLAUDE.md hard rule)
*.local.*
*.pfx
*.p12
*.jks
*.pem
*.key
/serverCommand
/buildAndRun.bat
