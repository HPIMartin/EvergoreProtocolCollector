# 07: Glossary

> **ADAPT:** this doc is a skeleton: fill in your domain terms. It is most valuable where the domain
> language differs from English (the code stays English, but domain proper nouns are kept verbatim)
> or where a term means something narrower in this project than in general use. Introduce each term
> once in [project-overview.md](project-overview.md) or [domain-model.md](domain-model.md) and keep
> the full mapping here; other docs link here instead of re-explaining.

## Domain terms

> **ADAPT:** one row per term: the domain term as it appears in code/data, and its meaning. Group
> into several tables (one per term cluster) if the list grows. Example row from the origin project,
> a German browser game: | Einlagerung | Deposit into storage (the `TransferType` enum constant) |.

| Term | Meaning |
|------|---------|
| | |
