# LF everywhere (engineering-handbook §7); binaries marked so they are never eol-normalized.
* text=auto eol=lf

# ADAPT: extend with your project's binary types.
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.zip binary
*.jar binary
