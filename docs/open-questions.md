# Open Questions & Decisions Log

Decisions and the questions that shape the plan. An answered question moves to **Decisions** with
date and rationale. Agents: read this before changing scope. Entries stay terse: decision + why +
where codified. The KB doc is the canonical home of each rule; this table is the dated index, not a
second copy.

## Decisions (answered)

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-13 | Created the knowledge base under `docs/knowledge-base/` and this backlog/log under `docs/` | Durable, agent-readable findings; avoids re-scanning |
| 2026-06-13 | **Q1 (Scope):** feature-parity dashboard (Epic E) + clean-code craftsmanship + the project doubles as a *showcase of working with Claude*; the KB documents how the code should look and how we develop | Author wants both the product and an exemplary way of working → `engineering-handbook.md`, `working-with-ai-agents.md`, `/CLAUDE.md`, Epic G |
| 2026-06-13 | **Q2 (Status):** game is live but this is primarily learning/fun; **no production pressure** | Security/ops (Epic C/F) stay good-practice showcase, not urgent |
| 2026-06-13 | **Q3 (Uncommitted diff):** normalize whitespace to **LF** (`.gitattributes`, own commit), then commit the real storage feature cleanly | Author wants Linux line endings and a clean history |
| 2026-06-13 | **Q4 (Architecture depth):** targeted fixes first; **defer** the full `domain/application/adapters` repackaging (backlog D3) until the core is test-covered | Restructure on top of a green suite, not before it |
| 2026-06-13 | **Language:** conversation in German; **code, comments, identifiers and docs in English** (German only for domain terms / item names) | Author preference |
| 2026-06-13 | **Commit workflow (strict):** propose **one** single-line, present-tense-verb message → author confirms → commit; **no body, no footer; never `git push`** | Pushing is the author's decision alone → CLAUDE.md, handbook §7 |
| 2026-06-13 | **TDD/BDD cycle:** red→green→refactor→commit; BDD Gherkin `@Ignore`-first → commit, then activate & drive green via TDD | Author's process → handbook §4/§5 |
| 2026-06-13 | **Multi-agent team:** Planner = author+Claude (Opus); subagents `implementer` (Sonnet), `falsifier` (Sonnet, feature-end), `reviewer` gate (Opus, per feature-commit); pre-approved micro-commits; agents never push | Built in `.claude/agents/` → multi-agent-playbook.md |
| 2026-06-13 | **Dev environment:** fully virtualized via devcontainer, **nothing native on the host**; agents run in-container | JDK upgrades without local installs → dev-environment.md |
| 2026-06-13 | **Git permissions:** `git add`/`commit` and read-only git prompt-free; **`git push` hard-denied** in `.claude/settings.json` | Pushing stays a manual, deliberate act |
| 2026-06-15 | **Migrate the build Maven → Gradle (Kotlin DSL)** as the next major item; tooling kept minimal for the move (Spotless, JaCoCo, the failsafe split and the warnings flip land *in Gradle* afterwards) | Author no longer happy with Maven; low plugin count eases the migration |
| 2026-06-15 | **Warnings-as-errors:** compiler/lint warnings fail the build; default is **fix**, not suppress. Landed 2026-06-19 as `-Xlint:all` + `-Werror`, only `-serial`/`-processing` excluded deliberately; the Java-25 Netty native-access warning fixed by enabling access (`--enable-native-access=ALL-UNNAMED`), not muted | Golden rule, mechanically enforced; the gate proven real by a deliberate failing warning → handbook §3/§8 |
| 2026-06-15 | **Deployment target:** Docker image built locally, run on the author's **home server**; **no CI/CD for now** (optionally a local gate later) | Solo + local deploy; CI deprioritized (backlog A4) |
| 2026-06-15 | **No local setup details in committed files:** no absolute paths, usernames or host layout; machine-specific config lives in gitignored `*.local.*` files | The repo is a public showcase → CLAUDE.md, handbook §7 |
| 2026-06-16 | **No secrets, no personal/host data, ever (hard rule):** passwords, tokens, keys, credentials, real emails/usernames, host/infra paths → gitignored `*.local.*` or a secret store, never the repo; a leaked secret is **purged from history and rotated** | Author's hard rule → CLAUDE.md golden rules, handbook §7/§8 |
| 2026-06-16 | **Commit-log conventions:** a single `[doc]` prefix for pure documentation/process commits; code & build commits stay bare verb-first; docs describing a code change fold into that commit; grouping decided by a revert/cherry-pick cohesion test | Scannable `git log`, cohesive commits → CLAUDE.md, handbook §7 |
| 2026-06-16 | **Gradle migration scope & shape:** Maven→Gradle (wrapper 9.5.1) + Java 17→25 (toolchain) + Micronaut 3.8.4→4.10.3 incl. devcontainer and `Dockerfile`, landed as **one cohesive commit** (the phases weren't standalone, failing the cohesion test); **Micronaut 5 jump deferred** until 1:1 is re-proven (backlog H9) | Up-to-date tech but 1:1 preserved; one framework risk at a time |
| 2026-06-19 | **Shared Eclipse formatter:** one central `config/eclipse/formatter.xml` (tabs, `lineSplit=180`) consumed by VS Code, Eclipse, IntelliJ (adapter) *and* Spotless; format + organize-imports on save; **star imports forbidden** (later expanded to fully wildcard-free); only two deviations carried (chain-wrap one-per-`.`, no trailing method-body blank); `.editorconfig` considered and **dropped**; full reflow accepted: uniform enforced standard over hand-tuned wrapping | One standard across IDEs + build from one file; JDT is the only engine all consumers share → build-run-deploy.md |
| 2026-06-19 | **Braces → Checkstyle `NeedBraces`, single-purpose:** the first choice (VS Code `addBraces` cleanup) proved **non-existent** in redhat.java and was scrapped; Checkstyle covers only the brace gap the formatter cannot fill and is **not** a general linter (that earlier rejection stands) | Real enforcement + IDE support; one rule per tool, no overlapping systems → build-run-deploy.md |
| 2026-06-20 | **Acceptance test as a synthetic, committed fixture** (not a PII prod snapshot): `TestDataGenerator` writes `testdata.sqlite`; real server + real evaluator, stubbed scraper; assertions through the JSON envelope. **Code must be self-explanatory without external documents** (a backlog-ID test name was rejected) | Synthetic data is committable and reproducible with zero PII risk → testing.md |
| 2026-06-20 | **Branching, merge & review gateway:** two tracks, chosen up-front by the planner: *small* → directly on `main`, one approved commit; *large* → feature branch/worktree, agents self-commit each TDD step, falsifier + reviewer review the diff, **author + planner review the branch log as the final gateway**. Merge = **rebase + `--ff-only`**, no squash. Every `main` commit is **verified-good** (a red build, a fake-green commit and a reviewer-rejected commit all count as broken); a gateway defect is repaired **by rebase into the owning commit**, never a follow-up fix commit; rewording via scripted (non-interactive) rebase | Keeps the TDD cadence without N approval round-trips; the author keeps a single veto point and a linear all-green history → handbook §7, multi-agent-playbook.md |
| 2026-06-20 | **No logic in constructors; field assignment only.** Construct+initialize orchestration belongs in a static factory (the `Repository.get(...)` methods) or a lifecycle hook | Logic in constructors breaks testability and SRP → handbook §3 |
| 2026-06-20 | **Avoid `static`; treat it as a smell**, above all mutable static state; prefer DI / instance state. Accepted exception: a one-time **test-code** initializer that must run before the framework context boots | Crystallised from a mutable static stomping across two boot-test contexts → handbook §3 |
| 2026-06-20 | **Per-class JVM test forking (`setForkEvery(1)`) introduced, then removed the same day:** once table creation moved into the repo factory and the mutable static delay became per-context config, boot tests share one JVM safely (also removed ~240s test waste) | The fork was a crutch for nondeterministic startup, not a fix; superseded by deterministic production ordering |
| 2026-06-21 | **`Category.storage` removed (dead field):** never read; the recipe-based valuation already yields identical values, pinned by an all-items golden master. The hunt-loot estimate stays a separate open question (D-4, gates E3) | Vestige of a superseded valuation model; zero value change, verified 1:1 |
| 2026-06-22 | **Durable docs must stand on their own; no dead backlog-shortcode references:** identify things by symbol/behavior/file/concept; a shortcode only as secondary pointer to a **still-live** item; completing an item removes its row and every referencing shortcode | Completed items leave the backlog, so bare shortcodes go dangling and defeat "docs are knowledge" |
| 2026-06-22 | **Prefer immutable `record`s; no getter/setter boilerplate; `Logger` last in constructors** | Less boilerplate, immutability by default, scannable signatures → handbook §3 |
| 2026-06-26 | **Tests must be deterministic; never wall-clock-dependent:** wait on a real signal (no-timeout latch, released on success *and* failure paths); a timeout/retry is a mitigation, not a fix | Must pass on any hardware; crystallised replacing a boot-signal poll timeout with a latch → handbook §6 |
| 2026-06-26 | **Code/build is the single source of truth; docs must not duplicate volatile facts** (versions, dependency lists, counts); prose points at the code | Restated facts rot silently and had already diverged → handbook §3 |
| 2026-06-26 | **Commit `gradle/gradle-daemon-jvm.properties` deliberately:** pins the daemon JVM portably, no host-specific or secret data | Reproducible and machine-independent for any checkout |
| 2026-06-28 | **Permission config unified for the worktree flow:** one portable committed wildcard `Bash(git -C *.claude/worktrees/*)` guarded by `-C` deny variants (`push`, `reset --hard`, `clean`); genuinely portable rules promoted into committed `settings.json`; session-/host-pinned cruft emptied from `settings.local.json` | The "always allow" flow kept writing dead host-pinned rules, one per worktree, re-prompting every time → handbook §7, working-with-ai-agents.md |
| 2026-07-03 | **Falsifier panel, gate escalation & session-model doctrine:** the falsifier splits into two cheap-tier lenses (`falsifier-domain`, `falsifier-robustness`); gates escalate per feature via a one-off `model` override to the strongest tier the plan offers (Fable when available, else Opus); the main-session model is picked per session job (strongest for planning/review, smaller for routine TDD) | Two cheap lenses cost about one mid-tier run and add diversity; session limits mean spend goes where it converts (planner + gates) → multi-agent-playbook.md, working-with-ai-agents.md |
| 2026-07-03 | **Setup-review triage (author calls):** the bug track (malformed date, watermark, WebDriver leak) goes before the Gildenmehrwert metric; the bundled webdrivers and the local-browser machinery are removed, then the author rewrites history himself; `Erde-Eibenlanze` is the real spelling (the code has the typo); python stays in the devcontainer (fix the nested features object); Dependabot adds `docker` + `gradle`; hook activation outside the container becomes a documented one-liner; the Claude home stays a bind mount (existing defenses judged sufficient); the falsifier panel stays read-only (scratch counter-tests via Bash); the record-equality tests are deleted; the ArchUnit inward-only rule extends to the core; a KB citation guard is added; the service runs without a reverse proxy; schema migrations get a real framework (the game serves only 30 days of logs, the DB is the only history); `git-state.md` becomes `git-conventions.md` | Full setup review 2026-07-03, findings triaged as multiple-choice with the author; fixes run through the normal pipeline → backlog D10-D12, F1, G18-G19, H11 + the build hardening (landed 2026-08-01) |
| 2026-07-04 | **Vulnerability scan: OWASP dependency-check → Trivy (binary, not Docker):** `./gradlew vulnScan` builds a CycloneDX SBOM (`org.cyclonedx.bom` plugin) and scans it with the `trivy` binary installed by a devcontainer feature; the NVD feed, API keys and the scan-secrets machinery (incl. `secrets.local.properties.template`) are gone; report-only by default, gated via `vulnScan.failOnSeverity`. Closes the prebuilt-NVD-DB-image evaluation: the Docker-image variants (owasp/dependency-check, trivy image) lost to the static binary because the devcontainer has no Docker | The NVD datafeed download was slow and kept aborting (author 2026-07-04); Trivy's DB is small, keyless, pulled from an OCI registry and cached under `~/.cache/trivy` → build-run-deploy.md |
| 2026-07-04 | **Frontend stack: React 19 + TypeScript + Vite** (Vitest/RTL, ESLint `typescript-eslint` strict + `eslint-plugin-import-x` boundary rules, Prettier), resolving **D-6** in favor of a JSON API + SPA over more HTML-string templates; built as three worktree strands (`frontend-build` → `json-api` → `spa-views`) | Small single-guild dashboard: React's ecosystem and Vite's fast dev/build loop fit the TDD cadence better than the previously-undecided Angular option → frontend.md |
| 2026-07-04 | **The SPA recreates Evergore's visual style itself; no game assets (images/CSS/markup) are copied into the repo** | Keeps the repo a clean-room showcase and avoids redistributing the game's assets |
| 2026-07-04 | **The current endpoint paths stay** (`/overview`, `/avatars/{avatar}/bank`, `/avatars/{avatar}/storage`) **and become the SPA's client-side routes** rather than being renamed | Preserves existing bookmarks/links; the SPA shell already serves at `/` and falls back to it for unknown navigation paths (strand `frontend-build`) |
| 2026-07-04 | **Token-filter scope inverts to `/api/**` only, deferred to strand `json-api`**; once inverted, `/swagger/**` etc. become public since they no longer sit behind the same gate | The SPA shell and static assets must load without a token; only the JSON API needs auth; keeps strand `frontend-build` scoped to build wiring only. **Superseded 2026-08-05** by the default-deny decision below: inverting to `/api/**` only would have left every unmapped path and both legacy HTML pages public |
| 2026-07-06 | **The frontend's npm dependency scan uses `trivy fs --scanners vuln --skip-dirs node_modules`, not `npm audit`** (re-planned, replacing an earlier `npm-audit`-task plan) | One scanner (Trivy) covers both the JVM (CycloneDX SBOM) and npm dependency graphs under a single `vulnScan.failOnSeverity` gate, avoiding a second tool/output format → build-run-deploy.md, frontend.md |
| 2026-07-06 | **Skills review (`/pause`, `/continue`, `/optimizeSettings`):** slash commands migrated from `.claude/commands/` to `.claude/skills/<name>/SKILL.md`; `/pause` runs fully autonomously: it signals running subagents to finish only their current micro-step (never kills, never waits for full task completion) and parks uncommitted work as ONE local `[wip]` commit (`--no-verify`, the one sanctioned bypass) that `/continue` must resolve first through the confirmed-message gateway; `/continue` slimmed to KB pointers (no rule copies) and starts without a go round-trip when the backlog "▶ next action" is unambiguous; `/optimizeSettings` additionally scans recent session transcripts under `~/.claude/projects/` | Duplicated rule copies drift (KB-first); an unambiguous next action was approved when written down; compaction hides earlier approvals; at pause time the author may have no time for input, so the commit gate moves to /continue while `[wip]` keeps the tip visibly non-deliverable → handbook §7, `.claude/skills/` |
| 2026-07-11 | **Permission tiering (autonomy-first, ground rules hold):** grow the agent's autonomy by **generalizing** observed allows into portable rules and **never blindly reverting** them (even ones a parallel session added); but destructive/irreversible/exfil-capable ops are **never a blind `allow`**. This pass promoted `java`/`javac`/`stat`/`git cherry-pick`/`nohup ./gradlew`/`xargs stat`/`xargs echo`/single-URL-localhost `curl`/`Read(/usr/lib/**)` → allow; `git checkout`/`git -C * checkout`/`dd`/broad `xargs`/`curl` → ask; `rm:*` → deny (the agent never runs `rm`, it hands over the command). A localhost `curl` allow cannot bound curl's multi-URL exfil, so single-URL-localhost-only is an **agent behaviour rule** (proper fix later: a PreToolUse hook, backlog G7/G13) | The "always allow" flow kept repolluting the committed `settings.json` with un-portable, path-bearing rules + tabs→spaces; codifying the tiering **and the autonomy-first / don't-revert principle in the `optimizeSettings` skill** stops future runs from reverting it → `.claude/skills/optimizeSettings/SKILL.md`, `process-learnings.md` |
| 2026-07-17 | **Roadmap & risk register as living docs:** `docs/roadmap.md` (milestone order + milestone-level acceptance; items and their acceptance stay in the backlog) and `docs/risks.md` (risks + countermeasures); completed milestones and handled risks are removed like backlog rows | Sequencing and risk knowledge had no single home; separate files keep the backlog item-scoped (DOC-4/DOC-5) → roadmap.md, risks.md |
| 2026-07-17 | **Data integrity before the dashboard:** the watermark rework plus the catalog fix and the domain confirmations precede the dashboard strands | Until fixed, every aborted run permanently under-counts and the 30-day log window makes the loss irreversible; the dashboard would render wrong numbers |
| 2026-07-17 | **Falsifier panel: third lens + per-surface composition:** `falsifier-frontend` joins the panel; a gate spawns only the lenses matching the touched surface (backend → domain+robustness, frontend-only → frontend+robustness, full-stack → all three); pure `[doc]` changes gate on the `doc-reviewer` alone (the `reviewer` only on genuine process doubt) | Gate effort should track what the change can break: a backend diff gains nothing from a frontend lens, a doc diff nothing from falsifiers; spend stays where it converts → multi-agent-playbook.md, `.claude/agents/falsifier-frontend.md` |
| 2026-07-17 | **Gate escalation extends to auth/security-surface changes** (filters, token handling, what is publicly exposed) | The `/api/**` token-filter inversion is an auth change yet matched no escalation trigger; a security regression is as costly as a valuation one → multi-agent-playbook.md |
| 2026-07-17 | **DB backup stays a monthly manual routine (author); no automation item** | The cadence suffices for a hobby: after a restore the 30-day log window re-scrapes most of the gap; revisit if the cadence slips → risks.md |
| 2026-07-17 | **Parser domain confirmations: `Einzahlung` stays mapped to `EINLAGERUNG`; the `+1` modifier is value-neutral and keeps merging with its base item** | `Einzahlung` occurs in real protocols but warrants no distinct behavior (author); a `+1` item is value-equal to its base, so merging loses nothing; both get pinned by contract tests and documented in domain-model.md |
| 2026-07-17 | **Meta sums: full recompute per run replaces the planned watermark rework** (every evaluation recomputes the per-avatar sums from all stored entries; `last_updated` becomes display-only, written after a successful run; **its meaning and write condition superseded 2026-09-03**, see below) | Ingest dedup never used the watermark and the DB keeps every ingested entry, so recomputing is idempotent, removes the DST/skew/same-minute/partial-write defect class at the root and heals historical drift (incl. zero-valued catalog misses) automatically; per-run cost is negligible at guild scale → domain-model.md |
| 2026-07-17 | **Ingest dedup covers the whole scraped window, not just its newest minute:** the extractor ingests the surplus of the scraped rows over `repo.getAllSince(min scraped timestamp)` (occurrence-counting, so two identical same-minute entries stay legitimate); this heals a still-visible protocol entry that never reached the database (e.g. an earlier, buggy strict-cutoff scrape), however far in the past it sits relative to the stored max, not just at the exact boundary minute. Extended to the whole window 2026-07-19 (author) | Without it the "no known silent-loss path" claim would be false, and the loss is permanent (30-day log window); a boundary-only fix still misses a still-visible row that is older than the stored max → architecture.md |
| 2026-07-18 | **Bank withdrawals are worded `Entnahme` in the protocol; `Auszahlung` does not occur there** (it is only the Google Sheet's column label); an unmatched timestamped protocol headline now warns loudly instead of silently dropping | A silent drop systematically understates sums; the falsifier proved the drop path → glossary.md, `EntryFactory`/`EntityParser` |
| 2026-07-19 | **Timestamps move to an epoch/UTC storage format (backlog D14), gated on the migration framework (D10); no interim code change** | The falsifier panel proved the default-TZ `DATE_STRING` storage breaks round-trip equality and the lexicographic max in a DST fall-back hour, and the window dedup makes value equality load-bearing; fix the format at the root instead of pinning the container TZ (author); until then the service runs under a fixed DST-free TZ (container default UTC) |
| 2026-08-01 | **The devcontainer gets `firefox-esr` as the interim browser; H2's remote `selenium/standalone-firefox` stays the planned end state** | Makes the scrape path locally runnable *today* with no code change (`Browser.DOCKER` builds a local headless `FirefoxDriver`, Selenium Manager fetches the geckodriver). H2's `RemoteWebDriver` rebuild is a separate, testable change; it then drops `firefox-esr` from the image again. Alternatives offered: do H2 now instead, or leave the path untestable |
| 2026-08-01 | **Per-class test forking (`forkEvery`) is removed for good and guarded in the build** (build-performance plan S4; confirms the 2026-06-20 decision, which a later feature commit had silently reverted). The suite runs in one JVM: `cleanTest test` **1m01s** instead of ~9 min, green in 5 consecutive runs, 1 fork instead of 198. Alternatives offered: parallel forks, or a split boot-test suite — both rejected as workarounds for a problem that does not exist | All three suspected causes were disproven: `BootSignalRecorder` has no static state, `Configuration.useInMemory`/`DATABASE_TEMP_SQLITE` are unwritten *instance* fields, and each `@MicronautTest` already owned its own SQLite file. A worktree at the very commit that (re-)introduced the fork is green without it. Gradle restarts the JVM per compiled test **class file** (198, only 28 with tests) and `--tests` filters inside the worker, so a single focused test cost 19m33s — real TDD was impossible → testing.md, `build.gradle.kts` |
| 2026-08-01 | **SonarLint is declared as a VS Code extension, not as the third-party `sonarlint` devcontainer feature** | That feature is a no-op (its `install.sh` only `echo`s; its whole effect is two extensions plus a `dependsOn` on `node`), so it added supply-chain surface for nothing. Its old GPG breakage came from the `node` feature's yarn apt repo, which node 2.x no longer uses. The real static-analysis gate remains backlog **G6**. Alternatives offered: re-add the feature, or keep deferring |
| 2026-08-01 | **TypeScript stays on 6.x; the TS 7 bump waits for typescript-eslint support.** The frontend npm refresh raises every declared range to the version it locks, so Dependabot's in-range PRs (eslint, prettier, vite, typescript-eslint) are superseded rather than merged; only the TS 7 PR stays open. Alternatives offered: run TS 7 side-by-side with the `@typescript/typescript6` alias feeding the linter, or take TS 7 and drop typescript-eslint from the lint config | Reproduced against the real config: `tsc -b` passes under 7.0.2, but `eslint .` exits 2 with `typescript-eslint does not support TS 7.0` (peer range `>=4.8.4 <6.1.0`), so `:frontend:npmLint` and the whole build go red. The cause is upstream and not fixable here: TS 7.0 ships no stable programmatic API (it returns in 7.1) and typescript-eslint closed the support request as not planned. TS 7 buys compile speed, and `tsc -b` costs about a second on this SPA, so a dual-compiler setup would be pure overhead → `frontend/package.json` |
| 2026-08-04 | **JSON API wire shape:** resource-oriented and versioned (`/api/v1/avatars`, `/api/v1/avatars/{avatar}/bank\|storage`); English field names spelled correctly (`withdrawn`, `deposited`); every collection answers the same envelope (`page`, `size`, `totalCount`, `items`), the overview additionally carrying `lastUpdated`; instants as ISO-8601 UTC and `transferType` as `DEPOSIT`/`WITHDRAWAL`, both localized by the client. Alternatives offered: mirroring the HTML routes, German sheet terms, keeping the `withdrawl` typo, pre-formatted timestamps, paging deferred to `spa-views` | The typo must not reach a public contract, so renaming it internally stays a non-breaking change; `totalCount` lets the SPA size its navigation instead of guessing from a short page; a version prefix makes the client renameable later → frontend.md |
| 2026-08-04 | **No wire or column name is ever derived from a Java identifier.** Every wire record component carries an explicit `@JsonProperty`, every `@DatabaseField` its `columnName`, every `@DatabaseTable` its `tableName`, and `RenameSafetyTest` fails the build on a missing one. `transferType` goes out as a `String` filled by `TransferTypeWireNames`, a visitor over the domain enum, so the German domain constants stay renameable too. Alternatives offered: a Jackson mixin per record (records stay import-free, but a rename is then only caught by the acceptance test, and Micronaut 4.10 ships no `@JacksonMixin`, so it needs a hand-wired `BeanCreatedEventListener`), or an explicit mapper to a `LinkedHashMap` (strongest guard and zero foreign imports, but the responses lose their OpenAPI schema) | A rename must never be a breaking change, and an implicit field-name mapping makes every rename one. There is **no** JDK-standard annotation for this: JSON binding never landed in Java SE, and `jakarta.json.bind`'s `@JsonbProperty` is not read by Jackson, so it would pin nothing. The coupling can only be placed, not avoided, and it sits where the name it pins sits, in the adapter layer that ArchUnit already allows framework imports in → frontend.md |
| 2026-08-04 | **The token scope is default-deny, not an enumerated deny list** (supersedes the M2 criterion "invert the filter to `/api/**`"): every path needs a token except the ones listed under `evergore.security.public-paths`, matched by `PublicPaths` against the canonicalized path. The list lives in `application.yml` next to the static-resource mappings it mirrors. Alternatives offered: a `@Public` annotation on the exempt controllers plus a code list for the static mappings, or the same list as a Java constant | An enumerated protect-list cannot be maintained by a human: a new controller would be public until somebody remembers it, which is a silent hole rather than a visible failure. An annotation covers only two of the nine exemptions, because `/`, `/assets/**` and the OpenAPI UIs are static-resource mappings and `/health` is Micronaut's own endpoint, so there is no class of ours to annotate. Config binding puts the list in one place, changeable without a recompile → build-run-deploy.md |
| 2026-08-04 | **Public surface accepted:** `/`, `/index.html`, `/assets/**`, `/favicon.ico`, `/health` and the OpenAPI UIs (`/swagger/**`, `/swagger-ui/**`, `/redoc/**`, `/rapidoc/**`) need no token. Only `/`, `/index.html` and `/assets/**` were additionally free of the rate limit and the request log (**superseded 2026-08-14**: nothing is exempt any more), the rest are counted and logged like any request. **A tokenless deep link therefore answers 401**: the shell loads from `/` and the client carries `?token=` across its routes | The SPA shell must load before it can send a token, and its assets carry no guild data: the bundle is the same clean-room code that is in the repo. Rate limiting *those* would throttle a normal page load and logging them would bury the real request log in asset noise, which is not true of the remaining public paths. Everything that carries guild data sits behind the token, including every unmapped path → build-run-deploy.md |
| 2026-08-04 | **JSON serialization is pinned in `application.yml`, not left to the framework default:** `jackson.serialization-inclusion: ALWAYS` and `write-dates-as-timestamps: false` | Both defaults silently broke the agreed contract: instants went out as epoch decimals, and the non-`ALWAYS` inclusion dropped empty `items` arrays and would have dropped the explicit `lastUpdated: null` that means "no run yet" |
| 2026-08-04 | **Javadoc is an absolute no-go** and counts as a comment, not as documentation; the same goes for explanatory `//` lines. Non-obvious knowledge goes into a name, a **test name**, the KB or this file. `reviewer` and `implementer` now flag every doc comment a diff adds, not only "unnecessary" ones | The handbook's "no comments unless intent can't live in names/structure" already covered it, but the `json-api` strand grew Javadoc on eleven files and both `reviewer` and `doc-reviewer` passed it, so the rule needed to name Javadoc explicitly to be enforceable → engineering-handbook.md |
| 2026-08-04 | **Tests rank above production code, and every test is strictly arrange/act/assert** with the act as its own named value; an act buried inside the assertion is a review finding of its own. A custom assert helper is the alternative shape and holds the same structure inside. `junit-jupiter-params` is declared so one behaviour over many inputs is a `@ParameterizedTest` instead of a repeated assertion | The tests are the specification the author reads, so care spent there outranks care spent on the implementation; a one-line `assertThat(call().field())` hides both the act and what is actually being exercised → testing.md |
| 2026-08-05 | **A 404 on an entry resource means the avatar is unknown, not that this ledger is empty.** `/api/v1/avatars/{avatar}/bank\|storage` answers 200 with `totalCount: 0` and `items: []` for a known avatar whose ledger happens to be empty, and 404 only when the avatar has no row in either ledger. Alternatives offered: keep the literal HTML parity (404 for an empty ledger) and record the ambiguity, or answer 204 for the empty case | The falsifier proved it on the committed fixture: `Calix` has bank rows but no storage rows, so `Calix/storage` and `Nobody/storage` were the same 404 and a client could not tell "this member never stored anything" from "this member does not exist" without a second request. That contradicted the strand's own promise that every collection answers the same envelope, and it would have been a breaking change to correct after `/api/v1` was published. The legacy HTML pages keep the old 404 and disappear with `spa-views` → frontend.md. Accepted cost: on the empty path the controller counts both ledgers, so one of the two counts is taken twice; threading a second supplier through `pageOf` to save it would cost a sixth parameter on a method the reviewer already called too wide, and the query is one indexed count on a guild-sized table |
| 2026-08-05 | **Falsifier probes get a gitignored `src/probe/java` source set** with the test classpath and its own `./gradlew probe` task, deliberately not wired into `check`/`build`; the falsifier agents write throwaway code only there. The deletion rule gains one narrow scope: inside that gitignored directory an agent may delete files it created in the same session (**that carve-out is superseded 2026-08-06** by the `clearProbes` decision below; the rule keeps no exception). Everything tracked by git, and everything the author wrote, still gets handed over as an `rm` command. Alternatives offered: no repo footprint at all (probes in the session scratchpad against the compiled classes, which does not fit `@MicronautTest` server-boot probes), or a name-based exclusion filter inside the existing test source set | The rule protects the author's ownership of **his** code, and a subagent's throwaway probe is not that. What actually forced the round-trip was mechanical: a probe under `src/test/java` turns `./gradlew build` red and `git add -A` commits it, so the author's `rm` sat on the critical path of every gate round (twice in this strand). Moving the probes out of git's and the build's sight removes the *need* to delete rather than the *rule* against it; a leftover becomes harmless hygiene. The name-filter variant was rejected because a mis-named real test would then be silently skipped, which is a fake-green risk exactly where it hurts most → backlog G11 |
| 2026-08-05 | **The HTTP status carries no application text:** `onNoElementFound` answers a plain `status(NOT_FOUND)` (empty body, like the other three visitor methods), and an **ArchUnit rule forbids the reason-carrying `status(…, String/CharSequence)` overloads repo-wide**. Alternatives offered: a constant reason phrase, or a JSON error body; for the guard: a parameterized test over today's four visitor methods, both, or no guard | The requested avatar reached the reason phrase, so a percent-encoded `CRLF` in an unknown avatar's name made Netty refuse the response: a caller-triggerable **500 with an `error`-level stack trace** instead of a 404 with one `info` line (reproduced end-to-end before the fix). A constant phrase only restates Netty's default and keeps a second call site of the overload alive; a JSON body would contradict the 404/401 empty-body contract the client codes against. The value stays visible in the log through the request path, so nothing is lost. A test bound to today's visitor methods would not stop the next call site anywhere in `rest`, the mechanical rule does → `ApplicationExceptionHandler`, `NoApplicationTextInHttpStatusTest` |
| 2026-08-06 | **"Never run `rm` yourself" keeps no exception, not even for a gitignored probe:** the falsifier agents clear `src/probe/java` with a committed Gradle `Delete` task (`./gradlew clearProbes`). Alternatives offered: `git stash push --all -- src/probe/` followed by `git stash drop`, or no cleanup step at all (probes die with the worktree) | The rule protects the author's ownership of **every** file of his, and an exception phrased per directory is one mis-scoped deletion away from costing him work; a Gradle task carries its scope in reviewed, committed code instead of in a permission rule, and the agent still runs no delete command of its own. `git clean -fdX src/probe/` was not an option: `clean` sits on the permission deny floor, and deny wins over any narrower allow. A TypeScript equivalent stays open until a frontend probe is first needed → build-run-deploy.md, handbook §7 |
| 2026-08-07 | **DOC-4 exempts the agent briefs:** an operative instruction may stand verbatim in several `.claude/agents/*.md`, while its rationale stays in the KB alone. Alternatives offered: shrink each copy to a one-line pointer (still identical text), or keep the instruction in one brief only | A brief is a prompt, not a document: an agent reads its own brief and never its siblings', so "one home plus cross-links" would leave two of three falsifiers without the instruction. The repo already worked this way (the identical `Environment` and working-directory paragraphs), and the `doc-reviewer` flagged the pattern twice in one gate for want of the written exception → KB README DOC checklist |
| 2026-08-07 | **Strand `spa-views` splits in two parallel strands:** `spa-look` owns `ui/` and `index.css` (the theme and the presentational primitives), `spa-data-shell` owns `domain/`, `api/`, `app/` and `App.tsx` (wire types, fetching, routing, composition). The primitives are demonstrated by a committed **gallery page**, `frontend/gallery.html` plus `src/ui/gallery/`, served by the Vite dev server and absent from the production bundle because Vite builds only the inputs it is given, and `gallery.html` is not one | Two strands can run at once only along a boundary git can merge, and the layer table already draws one: `ui` imports nothing but `domain`, so a presentational strand touches no file the data strand owns. The gallery replaces a Storybook dependency: the primitives are pure props-in components, so one page with fixture rows shows every state without a second toolchain, and it stays reviewable because `npm run build` output does not change → frontend.md |
| 2026-08-07 | **The self-built Evergore look is fixed as three token groups:** a dark tavern colour climate (wood ground `#1b1410`, parchment text `#e8dcc0`, gold accent `#c8a04a`, green deposits against rust withdrawals), a compact table (32px rows, 13px type), and serif display type over a sans body with tabular figures. System font stacks only. Alternatives offered: light parchment or muted forest green; comfortable or spreadsheet-dense rows; all-sans or all-serif | The dark ground carries the game's tone while long number columns stay quiet, and compact rows put a guild-sized ledger on one screen without reading as a spreadsheet. Serifs supply the fantasy note where no digits are involved; tabular figures keep the value columns aligned under proportional type. Web fonts are out because the SPA must render from the jar's static resources with no external request, which is also what the no-game-assets rule (2026-07-04) is protecting → frontend.md, `frontend/src/index.css` |
| 2026-08-07 | **A session never relocates into a worktree:** `EnterWorktree` is denied in `.claude/settings.json`; a strand's worktree is created with `git worktree` and driven from the primary checkout by absolute path. `Agent` worktree-isolation for subagents is untouched, it does not move the session. Alternatives offered: keep the tool and live with the split history, or a doc-only rule without the deny entry | Claude Code files session history per working directory, so a relocated session lands in a second project directory: the `spa-look` session vanished from the resume list of the window that started it and was recoverable only by reading the transcript out of that other directory, which also rules out driving parallel strands from one editor window. A doc-only rule would repeat the slip, since the tool is one call away and its cost is invisible until the session is needed again. The relocation's own safety net (a hook refusing writes to the shared checkout) is replaced by the absolute-path discipline the cwd-drift gotcha already demands → engineering-handbook.md |
| 2026-08-07 | **The SPA carries no routing and no data-fetching dependency:** a hand-written router in `app` (path → a `Route` visitor, `popstate`, `pushState`) and a hand-written `useLoad` hook hold navigation and load state. The client takes `page`/`size` and shows `totalCount`, but has no paging controls yet. Alternatives offered: `react-router-dom`, TanStack Query, and full paging controls in this strand | Three static routes over three read-only views: either library adds a runtime dependency (and Trivy/Dependabot surface) for code that fits on one page, and TanStack's cache and retry would make the tests less deterministic. `?token=` propagation had to be hand-wired either way, so the library would have saved nothing that matters here → frontend.md |
| 2026-08-07 | **The legacy HTML pages keep `/overview` and `/avatars/{avatar}/bank\|storage` until they are deleted, so the SPA's deep links are shown through the Vite dev server** (`server.proxy` sends `/api` to `localhost:8080`); the packaged application serves the shell at `/` and reaches the same three views by client navigation. Alternatives offered: delete the HTML controllers in this strand, or give the SPA fallback precedence over their mappings | `SpaHistoryFallbackController` answers only on a 404 and both controllers are mapped, so a deep link to those paths serves the old page. Deleting them belongs with the strand that gives the SPA its optics, otherwise a working dashboard is replaced by a raw one; a precedence filter would keep two dashboards alive at once → frontend.md |
| 2026-08-07 | **The frontend test run is ordered after the Java suite** (`mustRunAfter(":test")` in `frontend/build.gradle.kts`) and the Vitest worker fan-out is bounded to two (`test.maxWorkers: 2`). Alternatives offered for the bound: the `threads` pool instead of forks, or a machine-relative `maxWorkers: '50%'` | This strand takes the frontend suite from one test file to fifteen, nearly all booting their own jsdom, while `org.gradle.parallel` runs `:test` beside `:frontend:npmTest`. Under that load whole files never *started* ("Failed to start forks worker", "Timeout waiting for worker to respond") while no single test failed, and the result XML stayed `failures="0"` with a short test count, so the run had to be judged by its count. The bound alone was measured insufficient (one green and one red `clean build`); standalone the suite has never failed, so the contention is the cause and the ordering the fix, with the bound left as the second net | 
| 2026-08-08 | **The two parallel SPA strands are integrated in `spa-data-shell`, not left side by side:** the views render `PageFrame`, `SortableTable` and `StatusPanel`; the data shell's own raw tables and its `berlinTimestampOf` are deleted, so one Berlin formatter (`ui/format.ts`) and one table remain. `SortableTable` gains a `link` column kind and `PageFrame` an `onFollow`, both rendering through `Link`. Alternatives offered: rebase only and integrate in a later strand, or merge just the literal duplicates and keep both table sets | `spa-look` landed first with the primitives but no data, this strand had the data with raw tables: side by side that is two table implementations and two identical formatters in one `ui` layer. The primitives had no way to link a row or to navigate without a full reload, which the data shell needs, so the integration is where those two gaps get closed → frontend.md |
| 2026-08-08 | **The two acceptance nets merge into one class, `ProtocolEvaluationAcceptanceTest`:** its three HTML cases were already covered, more sharply, by the JSON sibling, so the sibling's body becomes the surviving class and the two cases only the HTML class had (storage valuation at the `MetaInformationRepository` bean level, an unknown endpoint answering 4xx) move in with it. Alternatives offered: keep both classes and re-point the HTML one at the JSON API, or merge but keep the `...JsonAcceptanceTest` name | Re-pointing would have asserted the same three things twice and booted a second server for it; the name loses its qualifier because there is one acceptance class again → testing.md |
| 2026-08-08 | **`SmokeTest`'s three HTTP cases move to `/api/v1/**` instead of being dropped:** each seeds a row through its repository and reads it back byte-exact, one avatar name per test. Alternative offered: delete them as covered by the acceptance test | They cover a different arrangement than the acceptance test, which reads a pre-baked fixture: here the write path and the read path meet at runtime. Porting also retires `assertClosest` and `TestHelper`, the Levenshtein half of the deferred `SmokeTest` rework → testing.md |
| 2026-08-08 | **The production-snapshot check writes its artifact as the `/api/v1/avatars` response body** (`build/tmp/prodSnapshot/overview-after-recompute.json`), asserting it carries every avatar rather than a first page. Alternatives offered: a TSV of the recomputed sums, or keeping a rendered HTML artifact by rendering inside the check | The JSON body is the contract now, is pinned byte-exact by the acceptance test and needs no transformation in the check; rendering HTML would have kept a second renderer alive against the point of the deletion → testing.md |
| 2026-08-08 | **One browser-driven test proves the bundled SPA paints real API data** (`DashboardBrowserSmokeTest`, built in this strand rather than deferred): headless Firefox against the booted server, reading rows back through the `data-testid` hooks. Alternatives offered: file a backlog row and build it with the Selenium-service work, only demonstrate it once by hand, or both | Author question that surfaced it: nothing proved the dashboard shows a single row. The Vitest suite fakes `HttpGet` and the Java suite stops at the JSON, so both halves passed while the join was unproven; `firefox-esr` is already in the devcontainer image, so the test costs no new infrastructure and skips where no browser exists → testing.md, frontend.md |
| 2026-08-08 | **The 1:1 release gate becomes a value comparison, not a byte diff:** the candidate's per-avatar numbers are compared against the live instance's rendered pages, and the status contract (empty page 200-vs-404, anonymous `/health`) is excluded by design. Alternatives offered: compare release-over-release artifacts only, or leave the procedure and rename the artifact | The two sides no longer share a format, so a byte diff is impossible; the numbers are what the gate was ever about, and the independent SQL recomputation stays the ground truth → testing.md |
| 2026-08-09 | **All four parser-robustness findings land together, and the "does this change already-stored output?" question is answered before they land**, by checking the stored avatars of a production snapshot for a blank name or a type word. Alternatives offered: land the three output-neutral fixes and defer the avatar-regex one, or land all four against a one-off SQL reconciliation | Only the avatar-regex fix can change a stored entry's parse output, and an unequal re-parse is exactly what the ingest-window dedup turns into duplicates. The snapshot held neither, so nothing had to be reconciled. The check itself stayed out of the suite: against the synthetic fixture it can only ever be green, and the suite must not depend on a gitignored snapshot → domain-model.md |
| 2026-08-09 | **One version number in three places, SemVer without `-SNAPSHOT`:** `version` in `build.gradle.kts` is the source (`0.1.0` for the first release), the git tag is `v<version>`, the image tag is `protocolParser:<version>`; a build off an untagged commit gets `<version>-<short sha>`, so a bare version tag means exactly one source state. The commit that sets the number is the release commit and the tagged commit. Tagging and pushing stay the author's act, documented as a command only. Alternatives offered: SNAPSHOT between releases (Maven-style, two version commits per release), CalVer; for the first number `1.0.0` or `0.0.1`; a moving `latest` tag; and the git tag without the `v` prefix | Nothing is published to an artifact repository, so SNAPSHOT would gate nothing and CalVer would signal nothing about breakage; `0.x` is honest while the dashboard scope is unfinished. A `latest` tag was rejected outright because it makes `docker ps` unable to name the running stand, which is the same hole that left the documented rollback ("start the previous image tag") unexecutable — there was no tag → build-run-deploy.md |
| 2026-08-09 | **The running stand is identified by the image tag plus OCI labels** (`org.opencontainers.image.version` and `.revision`, the commit sha, set at `docker build`), and the container runs under the fixed name `protocolParser` | The labels answer "which stand runs" even for an interim image, where the tag alone cannot, and cost two build arguments instead of code. Serving the version from `/health` was offered and deferred: it needs the version threaded into the distribution plus tests, which this release does not wait for. The fixed container name is what makes the deploy and rollback commands runnable as written, without first hunting a container id → build-run-deploy.md |
| 2026-08-09 | **The Docker image and container are named `evergore-protocol-collector`, not `protocolParser`:** a Docker repository name may not contain uppercase letters, so the name follows the repository while the Gradle project and the distribution keep `protocolParser`. Alternatives offered: the lowercased project name `protocolparser`, or a namespaced `evergore/protocolparser` | Found by the release dry run: `docker build -t protocolParser:…` fails outright with "repository name must be lowercase", so the scheme as first written was unbuildable. Readability on a host running several containers beat keeping one string across Gradle and Docker; the namespace form buys nothing without a registry → build-run-deploy.md |
| 2026-08-09 | **The Docker build stage copies `config/`, keeping the image gated on the full `check`.** Alternatives offered: revert the gate to `clean test installDist`, or drop the in-image build and copy the locally built distribution | Commit `988194a` (2026-07-07) switched the build stage to `check` without copying the Checkstyle and Spotless configs, so **the image build has been broken since that day** and went unnoticed because no image was built in between. Reverting the gate would also gate the image on nothing from the SPA (`test` matches nothing under `:frontend`); copying the local distribution would make the image depend on a developer machine instead of the commit → build-run-deploy.md |
| 2026-08-09 | **The release ships the lowered numbers and the drop is announced in the guild, with no correction booking:** the live instance's incrementally maintained bank totals had drifted **upward** by `6,886,024` (+5.58 %) on deposits and `11,588` on withdrawals, and the recompute is the correct number. Alternatives offered: a correction entry per avatar, or postponing the release until the drifting path in the running build is found | The excess is phantom, not history the release discards, so a correction entry would only freeze it into the data for every future recompute. Nine avatars whose stored history begins well after `2023-01-22` carry `860,843` of it (Aqii, first entry 2026-05-19, runs 23.2 % over; Valtan Glutherz 22.4 %); the excess correlates with activity (r = 0.75 against active days) and, measured relative to volume, *negatively* with membership length (r = -0.50); the day-one avatars with little activity are off by exactly `0`; and the sheet's largest documented pre-2023 bank movement (Fugger, `247,053` withdrawn before July 2022) leaves no trace, live meta and SQL sum agreeing on him to the gold piece. No contiguous window of stored entries sums to any large avatar's excess either, which rules out one double ingest. Not derivable from the two snapshots: the drifting code path itself, and whether a small part of the excess is individually lost rows → testing.md |
| 2026-08-10 | **The acceptance fixture's storage-only avatar deposits a craftable item, not a raw one** | With a zero-value item her `0/0` overview row is true for two reasons the test cannot tell apart: the overview is bank-only, **and** the deposit is worth nothing. A craftable deposit (370.08 at bean level) leaves "the overview's `deposited` is bank-only" as the only reason. Alternative offered: keep the raw item and accept the ambiguity |
| 2026-08-10 | **The overview's name order stays code-point order for now; German collation is filed as its own item** | The order predates the union and belongs to the sorting contract, not to "a member is missing"; fixing it inside the union strand would widen a correctness fix into a contract change. Alternatives offered: sort with `Collator.getInstance(Locale.GERMAN)` right here, or only state the code-point order in the KB |
| 2026-08-10 | **The union matches avatar names exactly; case-insensitive matching is withdrawn and filed as E9** | Folding the case in the union alone trades a hypothetical duplicate row for a real wrong number: the ledger lookups and the meta keys still match exactly, so the merged-away spelling's entries are never valued. Alternatives offered: fold the case in the lookups and meta keys too (adapter plus stored-key change), or keep the folding and pin the resulting numbers |
| 2026-08-14 | **There is no unmetered surface: public means token-free and nothing else.** The rate limit and the audit log run for every path, so the SPA shell, `/index.html` and `/assets/**` are counted and logged like any request; the code-side skip list (`SpaStaticResourcePaths`) is deleted and `evergore.security.public-paths` is the only list with a special status. Consequence: `max-requests-per-interval` rises 5 → 30, because one cold page load is already about 5 requests (shell, bundle JS, bundle CSS, favicon, overview call) and the old limit would lock a normal user out for a minute | A second definition of the public surface could drift from the configured one: dropping `/assets/**` from `public-paths` would have left the assets token-protected but uncounted and unlogged. One surface and one list beat two lists plus a cross-check test. The asset noise in the log is accepted as the price → build-run-deploy.md |
| 2026-08-14 | **The per-IP counter map is bounded by an idle sweep plus a hard cap** (`evergore.rate-limit.max-tracked-clients`, `10000`): when a new client appears, clients whose interval elapsed and that are not blocked are forgotten (their counter would reset anyway, so no decision changes), and if the map is still full the least recently seen client goes. The bound wins over a running block, and any value that would silently disable the throttle (a request or client budget below `1`, a non-positive interval or block duration) is refused at startup. **No legitimate client is ever refused for capacity** | Alternatives offered: cap plus LRU only (keeps dead entries until pressure builds), idle sweep only (no hard bound against many distinct IPs inside one interval), or fail closed at the cap (a spurious 429 for a legitimate client, and a cheap DoS lever). Losing a block only matters after that many distinct IPs have been more recent, and per-IP limiting is the wrong defence at that point anyway |
| 2026-08-14 | **No nonce exemption for follow-up requests; auth, session and rate limiting move to backlog C11 (JWT).** Discussed and not built: a cookie-borne nonce, issued on a token-backed shell request, that would exempt the asset and API follow-ups from counting so only the "root request" counts. The clarification behind it stands and is carried into C11: for an *authenticated* user the interesting audit event is which overview or avatar he opens, not the resources his session then fetches, and the rate limit is aimed at scanner bots | The only carrier that reaches a browser-issued asset request is a cookie, which would give the service session semantics and a second credential type it deliberately does not have (HMAC-signed and IP-bound it needs no store, but it needs scope rules: a nonce that can be minted anonymously over `/` hands a scanner an unmetered surface for one request). The gain is small: the tokenless shell bootstrap over `/` fires four anonymous requests itself, so the anonymous budget must carry a page load either way, and content-hashed assets are re-fetched only on a cold visit. A token bucket (burst plus a low refill rate) buys the same "a page load is one spike" semantics while every request stays counted, and it belongs in the JWT rework rather than in front of it → backlog C11 |
| 2026-08-14 | **The request filter splits in two along its two jobs:** `RequestAuditLogFilter` (order 1, one `info` line per request) and `RateLimitFilter` (order 2, per-IP counting), with `TokenValidationFilter` moving to order 3 and `ClientIp` as the one place that names a client | `BrowserLoggingFilter` was the rate limiter in disguise, and a filter named after its log line hides the security behaviour. Alternatives offered: one filter named after both jobs, or `RateLimitFilter` keeping the log line inside. Both filters share the client identity, so the audit line and the counter key can never disagree |

| 2026-08-15 | **Commits are sliced before the first edit, and a track's shape follows the work.** A plan names the commits it will produce, each green and reviewable on its own; a rename, a behaviour change and unrelated documentation never share one. "Small track, one commit on `main`" is an estimate, not a constraint: when the work needs several commits, it moves to a worktree and the rebase gateway instead of inflating the one commit | The rate-limit change landed as one commit over 35 files because the track had been scoped as single-commit at the start. Clean code covers git as much as Java, and a commit that mixes a rename, a behaviour change, a measured correction of another backlog item and three unrelated doc rows cannot be reviewed or reverted as one thought → handbook §7 |
| 2026-08-15 | **An environment YAML carries only what it overrides.** Micronaut merges property sources, so repeating a base value in `application-test.yml` or `application-ratelimit.yml` is duplication, and it rots silently when the base changes | Found in the rate-limit change, where both test profiles restated `interval`, `block-duration` and the new client budget verbatim from `application.yml` → build-run-deploy.md |
| 2026-08-15 | **The test name carries the intent; `.as(...)` stays the exception.** An assertion message is for the case where the failure output alone would not identify which input failed, or where something is proven by absence; everywhere else the name says it and the message is redundant narration | A message that states what the name does not means the name is wrong, and the reader learns to skim both. Existing occurrences are swept under B8 → testing.md |
| 2026-08-16 | **The API token is rotated with the `0.1.0` deploy**, to a random value; the existing bookmark URLs are given up for it. Alternatives offered: keep the token (deploy one step shorter, bookmarks survive), or rotate **and** close the router's port forwarding so the service is LAN-only | The old token was `secret_token` — the literal placeholder the old stand ships **inside** the paging script of every detail page, on an instance reachable from the internet. Anyone who ever loaded a page holds it, so the token protected nothing. The release is the cheapest moment to fix it: the new SPA reads the token from the URL, so the placeholder disappears from the served page anyway. "Keeping the value stable keeps bookmarks valid" now costs a real secret, not just convenience → build-run-deploy.md |
| 2026-08-16 | **The container on the home server stays `epc`, and the docs are corrected to it** (image keeps the long `evergore-protocol-collector`). Alternatives offered: rename the container to the documented long name at deploy time, or replace the name in the docs with a `<container>` placeholder | Where a doc and reality disagree, reality wins and the doc is fixed in the same change (handbook §3). The docs claimed a fixed name that never existed on the target, and the placeholder variant would have cost the property the fixed name exists for: every deploy and rollback command copy-pasteable under time pressure. A container name is neither host nor user data, so the showcase rule is untouched → build-run-deploy.md |
| 2026-08-16 | **`v0.1.0` is tagged on the tip of `main`, not on the commit that set the version.** The rule that made the version commit the release commit is corrected: the version commit only *declares* the number, the tag names the state that passed the release gate and whose image is deployed. Alternatives offered: move the bump to the tip by reverting and re-setting it (two ceremonial commits today, and every later release would have to end on a bump commit), or tag the version commit as written | The version commit `30d4214` sits 20 commits behind the tip and is the commit whose **image build is broken** (fixed in `bfe0b28`); it also predates the credential injection, the global rate limit, the avatar union and the Selenium determinism work. Tagging it would point the release at a stand that was never built or run, while the tested image came from a different commit → build-run-deploy.md |
| 2026-08-16 | **The Evergore login is injected as two environment variables, not as a mounted file, and the `FileLoader` port stays out of it.** Chosen over a mounted `zugang.txt` and over a Docker secret: the mount buys only that the value stays out of `docker inspect`, while adding a second silent-mount failure mode next to the database one; a Docker secret means Swarm or Compose, which would rewrite the freshly rehearsed `docker run` deploy. `FileLoader` resolves every path against `evergoreFolder`, so it could not have loaded a mounted `/zugang.txt` unchanged anyway; it stays the geckodriver loader it is. Missing or blank values fail at **startup** (`CredentialsStartupValidator`), exactly like the API token | The image becomes secret-free, the deploy grows by two `-e` and nothing else, and a forgotten login is a dead container instead of a healthy one scraping logged out. Accepted cost: the values are visible in `docker inspect` and in the deploy shell's history, the same exposure the API token already carries on a single-admin home server → build-run-deploy.md |
| 2026-09-02 | **The corrected storage sums go live with the overview's storage columns and the change is announced in the guild; no correction booking** (closes the 2026-08-09 detail question): the recompute *raises* the stored sums by roughly 40 % (`storage_placement` `77,723,347` → `108,542,193`, `storage_withdrawl` `226,534,092` → `310,087,852`, all 42 avatars upwards) and six avatars stand at exactly `0.0` in both storage keys despite years of stored entries (Ivory, Malak Almawet, Maesch, Mightypanda, Elazia Kapp, Thyla Vom Moos, all last active 2023/2024) while their bank meta matches the SQL sum exactly, so the two key families were filled by different mechanisms and the storage one never covered the full history. The drifting mechanism itself stays unidentified, and the recompute heals it either way. Alternatives offered: decide when the feature lands, or investigate the drifting mechanism first as its own item | Same line as the `0.1.0` bank drop: the recompute is the correct number, so a correction entry would only freeze the wrong one into the data for every future recompute. Nothing served moves until the columns exist, which is why the announcement rides with the feature instead of standing alone → roadmap.md, M5's open author step |
| 2026-09-02 | **The overview grows to the sheet's columns 1-5 plus a guild-wide total row:** bank in/out and storage in/out as four separate columns, the net per avatar, and a total row over all avatars. Alternatives offered: per-ledger nets only (narrower table, but deposits and withdrawals stop being separable), or the four columns without a total row | The four sums are what a member reconciles his own row against, and a net alone cannot be checked against the sheet; the storage half is already computed and stored every run, so serving it is a wire change, not a domain one → frontend.md |
| 2026-09-02 | **The time filter is one parameter pair, `?from=`/`?to=`, and `from` may be relative** (`-24h`, `-7d`, with `to=now`), resolved server-side; presets are **mixed rolling and calendar-aligned** (24 hours and 7 days rolling, this week from Monday 00:00, this month from the 1st, all time the default), and the arbitrary date range ships in the same cut, so the former date-range item is **absorbed** rather than left beside it. Alternatives offered: a `?range=24h` shorthand next to `?from=&to=` (two mutually exclusive forms, 400 when both are given), or presets resolving to a concrete `from`/`to` in the address | A preset bookmark must keep its meaning: resolving "last 24 hours" into an absolute window at click time freezes the bookmark on that window, which defeats the reason the filter state is in the URL. One parameter pair keeps the contract narrow, at the price of a duration parser with its own 400 rules. No window can come from the meta store, which holds all-time sums only → backlog E12 |
| 2026-09-02 | **"Gildenmitglied des Monats" awards all four titles and shows the last completed month beside the running one:** Materialsammler (deposited item count), Jäger (the same over `JAGDBEUTEN`), Goldgeber (bank deposits), Wertschöpfer (net over the month). Alternatives offered: the last completed month alone (a title that stands for a month), or the running month alone | The two item-count awards need no valuation rule, so they sidestep **D-4** entirely and honour the gathering the value model rates at zero; the standing title plus the running standing costs one more aggregation over the same query → backlog E13 |
| 2026-09-02 | **The overview truth cut goes next** (the four ledger sums, the net per avatar, the guild-wide total row and German collation as one track), ahead of the ledger paging defect, the time filter and the easter egg; **B20 no longer leads the queue**. Alternatives offered: paging first (the one outright defect of the six user requests), the windowed aggregation first (most leverage, no visible result until a UI sits on it), or B20 first as the roadmap had it | The overview is the officer-visible payoff and the headline metric the sheet exists for, and three of the six new requests land in it; B20 blocks nothing and M4 is otherwise done → roadmap.md |
| 2026-09-02 | **The net per avatar is derived, never stored:** a framework-free `AvatarContribution` over the four ledger sums answers `net()`, and the read side maps it onto the wire; no fifth meta key. Alternatives offered: a stored `guild_value_<avatar>` written per run (the item's "compute & store" read literally), or deriving it in the SPA from the four sums | A persisted derivative can drift from its four summands, and a fifth key family would join D10's migration and D9's rename scope for what is one subtraction; the same record carries the windowed sums that E12 and the Wertschöpfer award need in Java anyway → domain-model.md |
| 2026-09-02 | **The guild-wide totals are computed server-side and ride in the overview envelope** next to `lastUpdated`, over every known avatar rather than over the served page. Alternatives offered: summing the loaded rows in the SPA (no wire change, correct while one page holds all 42 avatars) | `totalCount` already means the unpaged total, so an unpaged `totals` reads the same way; a client-side sum silently becomes a page sum as soon as the overview pages or a window applies, and a page sum that presents itself as the guild's is the one thing the overview must not produce → frontend.md |
| 2026-09-02 | **The last bank and storage activity is queried from the ledgers, not stored as meta keys:** one query per ledger yields the latest timestamp per avatar. Alternatives offered: two further meta keys per avatar written by the evaluator (a single read path, but stale until the next run) | Entry timestamps are real instants, whereas a meta key persists Berlin wall-clock text and would inherit exactly the `last_updated` ambiguity that D14 exists to remove; it also keeps two more key families out of D10's migration → architecture.md |
| 2026-09-02 | **The overview stays a flat eight-column table whose two activity columns carry the ledger links**, so the link-only `Lager` column goes and `Column`'s `timestamp` kind gains an optional href. Alternatives offered: seven columns with one merged activity column, or a grouped two-row table head mirroring the sheet's merged headers | Every column head then sorts something real, which retires the standing finding that the `Lager` head offers a sort it can never perform; it is also the smaller change to the table primitive, a second head row with column spans being the larger one → frontend.md |
| 2026-09-02 | **The overview's storage sums and net reach the wire as whole gold** (`long`), each sum rounded **before** the net and the guild total are computed from it, rather than as the value model's raw doubles. Alternatives offered: `BigDecimal` at two decimals (the catalog's precision as a stated contract, always in plain notation), or keeping the raw doubles and documenting them | Jackson renders a `double` through `Double.toString`, so every value from 10^7 upward, which is where the real per-avatar sums sit, would reach a client in exponential notation carrying float noise; the small fixture numbers that make the acceptance golden look clean are an accident of the test data. The sheet shows whole gold and the SPA already rounds to no decimals, so whole gold is what a member reconciles against, and rounding before summing keeps every row internally consistent and the total row exactly the column sum of the rows above it → frontend.md |
| 2026-09-03 | **The meta store stays the overview's producer; the torn read is closed at the port, not by aggregating the ledgers live**: the recompute hands its whole result to `MetaInformationRepository.add` as one batch written in one transaction, and the read side answers a response from one `MetaInformationSnapshot`; the per-key `get` is gone from the port. **Live aggregation was measured, not estimated,** on the production snapshot (42 avatars, 245,072 rows): a meta-store read costs ~120 ms, a live aggregation 24.4–25.6 s as the code stands and 1.23–1.61 s at best case (single scan, hashed catalog), growing linearly with the log forever | Keeps the **1:1-verified** quantity where B19 will script it, keeps a correctness fix independent of **E12**'s performance design, and needs no answer to **D12** (the meta port has one implementation, unlike the two ledger repositories). E12 still owns the windowed aggregation; all-time simply is not served from it |
| 2026-09-03 | **A recompute isolates failures per avatar; the failing avatar keeps its stored sums and is named in `/health`** (falsifier finding, same day): the batched write would otherwise have turned one unreadable ledger row into a permanent guild-wide freeze, because `getAllFor(avatar)` still dereferences `timeStamp` unconditionally and throws before the batch is written | Narrows a full-run freeze back to per-avatar staleness without giving up the single transaction the torn-read fix relies on, and follows the existing unknown-item reporting path rather than inventing a second one |
| 2026-09-03 | **`last_updated` means "when data was last scraped from the game", and is stamped on every completed run, including one in which an avatar failed to recompute** (author clarification): it is an operator's datum, not a per-row freshness claim | Scrape time and recompute completeness are separate facts, and tying the timestamp to the second would make `/health`'s `lastSuccessfulRun` and the overview's `lastUpdated` contradict each other, and could leave `lastUpdated` null beside real sums on a guild that never had a fully clean run. "How current is this member's row" is answered by that row's own two last-activity columns instead. Moving the timestamp off the members' overview is **E14** |

## Detail questions (lower priority, to confirm later)

- **D-1 (Sheet columns 8 & 9):** exact meaning/formula of the 2nd percentage and the small integer
  count? (col 7 ≈ hunt-loot ÷ storage-deposits is confirmed; 8 & 9 are not, see [google-sheet](knowledge-base/google-sheet.md)).
- **D-2 (Other workbook tabs):** does the spreadsheet have tabs (raw protocol, item price list,
  per-month history) beyond `gid=937183112`? The CSV export only sees one. Scrape/import them?
- **D-3 (Item value source of truth):** are `EvergoreItem.marketValue`s authoritative, or were the
  sheet's values maintained separately (and possibly drifted)? How are new items / price changes maintained?
- **D-4 (Hunt-loot valuation rule):** the sheet estimates hunt-deposit value (col 6) but code gives
  `JAGDBEUTEN` storage value 0. What's the intended rule? (gates E3; `Category.storage` was removed as
  dead, see the Decisions entry, so the hunt-loot estimate is the remaining, separate question.)
- **D-12 (How the overview shows the levy):** the total row adds measured bank gold to modelled
  material value and therefore reads `−80.545.764` while the guild's treasury stands at `+119.562.181`;
  the gap is the guild's own levy (`219.202.477`). Three variants were mocked up 2026-09-03, author
  choice open (gates **E15**): **(A, recommended)** a stat header — treasury / material balance / levy
  collected — plus a Beitrag↔Saldo switch, leaving the table's column count and density untouched;
  **(B)** a ninth column "Abgabe" between contribution and balance, which widens the table; **(C)**
  split only the total row into gold and material, the smallest change, which fixes the misleading
  statement but explains nothing. Renaming *Gildenmehrwert* to *Beitrag* / *Saldo* rides along in all
  three. Separate from **D-4**, which asks what raw deposits should be *worth*; this one asks how the
  resulting levy is *shown*.
- **D-9 (BDD tooling):** plain JUnit given/when/then, or a framework (Cucumber)? Assumed plain unless asked.
- **D-11 (ToS / PII, enterprise-audit Pitfall #7):** the tool scrapes evergore.de and stores *other*
  guild members' bank/storage activity. No production pressure (Q2), but a deliberate stance is due:
  is the scraping within the site's ToS, and is storing other members' activity acceptable for the
  guild's use? Low priority; logged so it isn't silently ignored.

## Assumptions currently baked into the plan (challenge if wrong)

1. Single-guild, low-traffic tool; correctness & maintainability ≫ performance.
2. The author wants to *continue* the rebuild, not abandon it.
3. The verified Gildenmehrwert formula (deposits − withdrawals across bank+storage) is the
   intended definition of member contribution.
