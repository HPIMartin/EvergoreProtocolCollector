# Open Questions & Decisions Log

A living record of decisions and the questions that shape the plan. When a question is answered,
move it to **Decisions** with the date and rationale. Agents: read this before changing scope.

## Decisions (answered)

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-13 | Created the knowledge base under `docs/knowledge-base/` and this backlog/log under `docs/` | The author asked for durable, agent-readable findings; avoids re-scanning |
| 2026-06-13 | **Q1 — Scope:** Feature-parity dashboard (Epic E) **+** clean-code craftsmanship **+** the project doubles as a *showcase of working with Claude*. The KB must also document **how the code should look and how we develop** (TDD/BDD/clean code). | Author wants both the product (replace the sheet) and an exemplary, well-documented way of working. → added `engineering-handbook.md`, `working-with-ai-agents.md`, `/CLAUDE.md`; new Epic G. |
| 2026-06-13 | **Q2 — Status:** Game is live but this is primarily learning/fun; **no production pressure**. | Security/ops (Epic C/F) stay good-practice for the showcase but are not urgent; we *can* run against live evergore.de when useful. |
| 2026-06-13 | **Q3 — Uncommitted diff:** Normalize whitespace to **LF** (`.gitattributes`, own commit), then commit the real 15-file storage feature cleanly (A1 → A2). | Author explicitly wants Linux line endings in the repo and a clean history. |
| 2026-06-13 | **Q4 — Architecture depth:** Targeted fixes now (PageSource port D1, real config/secrets C1–C3, unify visitors D4); **defer** the full `domain/application/adapters` repackaging (D3) until the core is test-covered. | Lower risk; restructure on top of a green test suite, not before it. |
| 2026-06-13 | **Language:** conversation in German; **code, comments, identifiers and docs in English** (German kept only for domain terms / item names). | Author's stated preference. |
| 2026-06-13 | **Commit workflow (strict):** propose **one** single-line, present-tense-verb message → author confirms → commit; **no body, no `Co-Authored-By`/footer; never `git push`.** | Author's rule; pushing is his decision alone. Encoded in `CLAUDE.md`, handbook §7, memory. |
| 2026-06-13 | **TDD/BDD cycle:** red→green→refactor→commit. BDD is doc/showcase-only: write Gherkin `@Ignore`-first → commit, then activate & drive green via TDD → commit. | Author's process; see handbook §4/§5. |
| 2026-06-13 | **Multi-agent team:** Planner = hat (you+me, Opus); subagents `implementer` (Sonnet), `falsifier` (Sonnet, single, feature-end), `reviewer`/gate (Opus, **per feature-commit**). Implementer makes pre-approved micro-commits; agents never push. | Q-round 2026-06-13. Built in `.claude/agents/` + `knowledge-base/multi-agent-playbook.md`. |
| 2026-06-13 | **Dev environment:** fully virtualized via devcontainer — **nothing native on the host**; agents run in-container. Devcontainer fixed now (Maven, JDK 17 pinned, m2 cache); Selenium-compose + Dockerfile de-hack deferred (Epic H). Sequence: fix+commit on host, then `Reopen in Container` for coding. | Author wants future JDK upgrades (e.g. Java 25) without local installs. |
| 2026-06-13 | **In-flight WIP:** resolve the entangled storage-calc WIP **in the container, verified** (split via `git add -p`; fix the dup-avatars bug, the `clickCookieShit` name, and the hard-coded Firefox path), **not** blind-committed on the host. Plan in `backlog.md` → Current status. | The WIP mixes concerns in one file and the build can't be verified on the host. |
| 2026-06-13 | **Git permissions:** `git add`/`commit` (and read-only git) allowed without prompts; **`git push` hard-denied** in `.claude/settings.json`. | Author's request; pushing stays a manual, deliberate act. |
| 2026-06-15 | **Migrate the build Maven → Gradle** as the next major item (before D2 and feature work). Reverses the 2026-06-13 "devcontainer fixed to Maven" stance. Keep the tooling surface minimal to ease the move: **defer** A7 (Spotless), G6+ (JaCoCo), H6 (failsafe) and the `failOnWarning` flip and **land them in Gradle**, not as new Maven plugins. Build DSL: **Kotlin (`build.gradle.kts`)**. | Author is no longer happy with Maven; a low plugin count eases the migration. |
| 2026-06-15 | **Warnings-as-errors:** compiler/lint warnings are build failures (`-Xlint:all` + `failOnWarning`); default is to **fix**, not suppress; obsolete lint excluded deliberately (e.g. `-serial`); ask the author per-warning when unsure. The flip rides with the Gradle migration (build config). | Author's rule — surfaces quality issues early; keeps the showcase build clean. Codified in CLAUDE.md golden rules + handbook §3/§8. |
| 2026-06-15 | **No local setup details in committed files:** no absolute paths, usernames, or host layout in the repo; machine-specific config (e.g. an absolute-path-scoped `cd` permission) lives in gitignored `*.local.*` files. | Author's **core rule** — the repo is a public showcase; keep committed config portable and free of personal environment details. Codified in CLAUDE.md + handbook §7. |
| 2026-06-16 | **No secrets, no personal/host data — ever (hard rule).** Broadens the 2026-06-15 rule to *all* sensitive data: passwords, tokens, keys/keystores, credentials, real emails/usernames, host/infra paths. Such data → gitignored `*.local.*` or a secret store, never the repo. | Author's hard rule; on a public repo a leaked secret must be **purged from history and rotated**, not just deleted. Codified in CLAUDE.md golden rules + handbook §7/§8. |
| 2026-06-16 | **Commit-log conventions:** a single `[doc]` prefix for pure documentation/process/meta commits (touching only `docs/`/`.claude/`/top-level `*.md`); code & build commits stay bare verb-first. Docs that describe a code change fold into that commit (no `Refresh docs after X`). A cohesion test decides grouping (can a part be reverted/cherry-picked alone?). | Author wants `git log` scannable (doc churn broke reading flow) and related commits cohesive. Reverses the prior strict "no prefixes" rule for the single `[doc]` tag only. Codified in CLAUDE.md + handbook §7. |
| 2026-06-16 | **Gradle migration target & scope:** execute the move as **Maven→Gradle (Kotlin DSL, wrapper 9.5.1) + Java 17→25 (Gradle toolchain, foojay-provisioned) + Micronaut 3.8.4→4.10.3** in one go, including the devcontainer (JDK 25) and `Dockerfile` (temurin-25 build → selenium-firefox runtime, `installDist`). The **jump to Micronaut 5** is deferred to the backlog, to be taken **only after 1:1 is re-proven** (H9). | Author wants up-to-date tech but must keep the app working **1:1**. MN4 is the proven line that already supports Java 25; MN5 (Apr 2026) is brand new — one risk at a time. |
| 2026-06-19 | **A7 — shared Eclipse formatter adopted.** A single central profile `config/eclipse/formatter.xml` (tabs, `lineSplit=180`, empty bodies compact, enum constants one-per-line) is consumed by **VS Code** (`java.format.settings.url`), **Eclipse**, **IntelliJ (adapter)** *and* the Gradle build (Spotless `eclipse().configFile(...)` + `importOrder` java→extern→`dev.schoenberg`→static-last + whitespace). VS Code does **format + organize-imports on save** (`[java]`). **Star imports are forbidden** — `starThreshold: 999999` (always explicit). The codebase was reflowed to this standard (38 files, max line = 180); build green. **Reversal of the initial "preserve manual wrapping" instinct:** the author chose *uniform + enforced standard* over hand-tuned line-art ("alles einheitlich, dafür erzwungener Standard"). `.editorconfig` was considered and **dropped** (VS-Code-only, Java-primary; its universal basics — trim/final-newline — moved to native VS Code `files.*` settings, so `formatter.xml` is the *single* source for Java format). All wildcard imports were then **expanded to explicit** (IDE organize-imports — Spotless can't resolve types; `removeUnusedImports` added to Spotless); codebase is wildcard-free, build green. **Next (separate):** formatter fine-tuning — chain-wrap per `.`, fewer blank lines, wrap `if`-bodies (brace-insertion is *not* formatter-doable — needs Checkstyle/cleanup, TBD). | Author wants one enforced standard across IDEs + build from one file. JDT is the only engine VS Code/Eclipse/Spotless all share. Resolves **D-10**. |
| 2026-06-19 | **H8 — warnings-as-errors landed:** `-Xlint:all` + `-Werror` on every `JavaCompile` in `build.gradle.kts`. The code was already warning-clean; only two categories are excluded deliberately — `-serial` (obsolete) and `-processing` (Micronaut/ORMLite/JUnit/ArchUnit annotations no processor claims, inherent to the stack). The gate was proven real (a deliberate `[cast]` warning fails the build). The Java-25 Netty native-access warning is silenced + future-proofed via `--enable-native-access=ALL-UNNAMED` on the test JVM and in the distribution start script (`applicationDefaultJvmArgs`) — not via suppression. | Author's golden rule, now mechanically enforced. `-Werror` over Maven-era `failOnWarning`; the two exclusions match the documented `-serial` precedent. Native access enabled (not muted) because the JVM will block these calls in a future release. |
| 2026-06-19 | **Formatter fine-tuning.** Tuning reduced empirically to two real deviations (chain-wrap one-per-`.` >180; no trailing-method-body blank); the rest were JDT defaults or not JDT-doable. | Keep the profile minimal/honest (deviations-only); only settings that actually change output are carried. |
| 2026-06-19 | **Brace strategy → Checkstyle `NeedBraces`** (build + IDE), **single-purpose**. The first choice (VS Code `addBraces` cleanup on save) proved **non-existent** in redhat.java 1.54.0 (no brace/block cleanup or source action), so it was scrapped. Checkstyle is introduced **only** for the brace gap (one rule), disjoint from the formatter (layout stays in `formatter.xml`); it is **not** a general linter. Considered: convention+review (no enforcement) and braceless-allowed (breaks the 100%-braced style) — both rejected; author wants real enforcement and IDE support (showcase). | Reverses the prior `addBraces` decision (false premise). **Checkstyle ≠ formatter:** it only *checks*, cannot auto-format, so it cannot replace the formatter; kept disjoint (one rule per tool) to avoid maintaining overlapping systems. The earlier "Checkstyle as general linter" rejection still stands; this narrow use is the only automated way to enforce braces the formatter can't insert. |
| 2026-06-16 | **Execution shape:** the migration was developed in phases (1: tool swap on MN3; 2: Java/Micronaut bump; 3: container rebuild) but **landed as a single cohesive commit** — the intermediate phases aren't standalone (e.g. Maven removed while the `Dockerfile` still calls Maven leaves the tree inconsistent), failing the revert/cherry-pick cohesion test. | Author's call: "phasenbasierte commits gehen nicht, weil sie nicht stand-alone sind." |
| 2026-06-20 | **D2 realized as a synthetic, committed fixture** (not a PII prod snapshot). `TestDataGenerator` writes `src/test/resources/testdata.sqlite`; `ProtocolEvaluationAcceptanceTest` boots the real server against a `build/` working copy, stubs the scraper, runs the **real** evaluator, and asserts overview + avatar pages via a dependency-free, restyle-proof `RenderedTable` helper, plus storage valuation at the meta-repo bean level (no endpoint surfaces it yet — Epic E1). **Code must be self-explanatory without external documents** — meaningful names that stand on their own (the initial `D2AcceptanceTest` name was rejected for referencing a backlog ID). | Author: synthetic + anonymised data makes the fixture committable and the test fully reproducible with zero PII risk; UI will be beautified later, so web assertions must be robust/centralised. The project must read on its own — names carry the meaning, not the tracker. |
| 2026-06-20 | **Development workflow — branching, merge & review gateway.** Two tracks, chosen **up-front by the planner** (announced, author can veto): *small / single TDD cycle* → directly on `main`, **one** approved commit; *large / multi-cycle or new BDD* → a **feature branch** where agents **commit each TDD step themselves** (protocol-conform messages, **no per-commit approval on the branch**), falsifier + reviewer review the branch diff, then **author + planner review the branch `git log` together as the final gateway**. **Merge = rebase + fast-forward only** (no merge commits, **no squash** save rare exceptions). **Every commit on `main` is verified-good — not merely green:** "broken" includes a red build, a *fake-green* commit the falsifier exposes (tests assert nothing real), **and** a commit the reviewer rejects for not implementing what it claims — none may land; a defect found at the gateway is repaired by rebase into the commit it belongs to **before** the merge (never a follow-up "fix" commit). Gateway message rewording uses a **scripted** (non-interactive) rebase via `GIT_SEQUENCE_EDITOR`/`GIT_EDITOR` (the agent has no interactive TTY); `git push` stays author-only. Worktrees enable parallel / benchmark runs. | Resolves the tension between "one approved message per commit" and "many small TDD commits": big features keep the real red→green→refactor cadence without N approval round-trips, while the author keeps a **single** veto point at the merge and a clean, linear, all-green history. `git rebase -i` was never a project ban — it's a harness-TTY limitation, and scripted rebase lifts even that for rewording. Codified in CLAUDE.md + handbook §7 + multi-agent-playbook. |
| 2026-06-20 | **No logic in constructors — field assignment only.** Constructors must not run logic or side effects (no IO, no `init()`/`ensureTable()`-style calls). "Construct + initialize" orchestration belongs in a **static factory method** (the `Repository` subclass `get(...)` methods — Effective Java Item 1) or a lifecycle hook. Surfaced when an `ensureTable()` call was put in the `Repository<T>` constructor and rejected. | Logic in constructors breaks testability and violates SRP/Clean Code; the author holds this strictly. The factory is the correct seam for build-then-initialize. Codified in handbook §3; demonstrated by the D7 repository change. |
| 2026-06-20 | **Avoid the `static` keyword — treat it as a smell, above all mutable static state.** Not a blanket ban (rare good uses exist), but the default is to avoid it: mutable statics create hidden cross-instance/cross-test coupling and break testability/isolation. Prefer DI / instance state / a proper seam; boot tests use `@TestInstance(PER_CLASS)` + instance fields instead of static signal flags. The one accepted exception is a static *initializer* doing one-time setup that must run **before** the framework context boots (seeding/deleting a test DB) — accepted because it's **test** code; in production, skeptical. | Author's rule, crystallised from the D7 bug (a `public static int DELAY_IN_SEC` stomped across two boot-test contexts). Codified in handbook §3. |
| 2026-06-20 | **Per-class JVM test isolation (`setForkEvery(1)`).** Two `@MicronautTest` boot tests sharing one JVM let one boot's timing perturb the other's, exposing a latent `SmokeTest` startup race (job evaluates before tables exist). Forking each class into a fresh JVM removes the cross-class interference. **Superseded by D7 (2026-06-20):** `setForkEvery(1)` was removed once D7 made table creation deterministic (tables created in the repo factory `get(...)`, `DatabaseStartupInitialization` deleted) and the global mutable `DELAY_IN_SEC` was replaced by a per-context `Configuration.getCollectorInitialDelaySeconds()`; boot tests now share one JVM safely (no shared mutable static), which also removed ~240s of per-test await waste in the acceptance test. | Lowest-risk fix that kept D2 strictly additive at the time; once the production ordering was made deterministic (D7), the forking crutch was no longer needed and the fast build was restored. |
| 2026-06-21 | **D-5 — `Category.storage` removed (dead field).** The per-category `storage` multiplier was never read: `getStorageValue()` is recipe-based, the three `storage==0` categories (`EDELSTEINE`/`JAGDBEUTEN`/`ROHSTOFFE`) are all `NOT_CRAFTABLE` so the recipe rule already yields 0, and the `0.6` on craftable categories was never applied. The field + its ctor param were removed; an all-items golden-master test pins `getStorageValue`/`getWithdrawlValue` unchanged (B5). | A vestige of an earlier `marketValue × storage` model superseded by the recipe-based valuation; removing it deletes a misleading artifact with **zero** value change (verified 1:1). The hunt-loot estimate (**D-4**, the sheet's "geschätzte Jagdeinlagerungen") is a *separate* gap that still gates E3. |
| 2026-06-22 | **Durable docs must stand on their own — no dead backlog-shortcode references.** In the KB, this Decisions log, and `process-learnings.md`, identify things by symbol/behavior/file/concept — not by a backlog shortcode (`D5`/`D7`/`B8`/…) that is removed when the item completes. A shortcode may appear only as an optional secondary pointer to a **still-live** backlog item. Applies going forward; sweeping the existing dangling references is tracked as **G14**. | Completed items are removed from the backlog (backlog-hygiene), so a doc that names only a shortcode becomes a dangling reference needing `git` archaeology — which defeats "docs are knowledge". Same principle as no-tracker-IDs-in-code, extended from code to docs. Raised by the author 2026-06-22. |
| 2026-06-22 | **Prefer immutable `record`s; no getter/setter boilerplate; `Logger` last in constructors.** Model value objects / DTOs / config as `record`s (or public-field classes for DTOs, as `BankEntry`/`StorageEntry`); record accessors are fine, bean getters/setters are not. A constructor's `Logger` dependency is always its last parameter. | Author's standards: less boilerplate, immutability by default, scannable constructor signatures. Codified in handbook §3 + memory. Surfaced reworking the C2 `SecurityConfiguration` (POJO→record) and trimming `AvatarController`. |
| 2026-06-26 | **Tests must be deterministic — never wall-clock-dependent.** A test must pass on any hardware (even a first-gen Raspberry Pi, just slower); correctness must never hinge on a `sleep`/`timeout` threshold. Wait on a real signal (e.g. `CountDownLatch.await()` with no timeout), released on both success and failure paths so a bad run fails an assertion instead of hanging. A timeout/retry is a mitigation, not a fix. | Author's core testing rule. Crystallised replacing the boot-signal poll timeout (a flake under CI contention) with a no-timeout latch in `BootSignalRecorder`. Codified in handbook §6. |
| 2026-06-26 | **Code/build is the single source of truth; docs must not duplicate volatile facts.** Versions (Java / Micronaut / Gradle), dependency lists, and counts live in `build.gradle.kts` / the code; prose points to them rather than restating them. Trimmed the stale "Java 17 / Micronaut 3.8.4" from the README/`CLAUDE.md`/KB taglines and `architecture.md` (real stack: Java 25 · Micronaut 4.10.3). | Restated facts rot silently and had already diverged. Author directive. Codified in handbook §3. |
| 2026-06-26 | **Commit `gradle/gradle-daemon-jvm.properties` deliberately (not gitignore).** It pins the Gradle *daemon* JVM to Java 25 via the foojay resolver for every OS/arch — portable, contains no host-specific or secret data, and supports the "no native toolchain on the host" goal. | A generated lock-style file, but one whose contents are reproducible and machine-independent; committing it makes the daemon JVM deterministic for any checkout. |

## Strategic questions — ANSWERED 2026-06-13

The four strategic questions were answered by the author (see the Decisions table above):

- **Q1 — Scope:** parity dashboard **+** clean-code craftsmanship **+** *showcase of working with Claude*
  (the KB documents how the code should look and how we develop). → Epic E in scope; new Epic G added.
- **Q2 — Status:** live but learning/fun; no production pressure. → C/F good-practice, not urgent.
- **Q3 — Diff:** normalize to LF then clean feature commit (A1 → A2).
- **Q4 — Architecture:** targeted fixes now (D1, C1–C3, D4); defer full repackaging (D3) until tested.

## Detail questions (lower priority — to confirm later)

- **D-1 — Sheet columns 8 & 9:** exact meaning/formula of the 2nd percentage and the small integer
  count? (col 7 ≈ hunt-loot ÷ storage-deposits is confirmed; 8 & 9 are not — see [google-sheet](knowledge-base/google-sheet.md)).
- **D-2 — Other workbook tabs:** does the spreadsheet have more tabs (raw protocol, item price list,
  per-month history) beyond `gid=937183112`? The CSV export only sees one. Should we scrape/import them?
- **D-3 — Item value source of truth:** are `EvergoreItem.marketValue`s authoritative, or were the
  sheet's values maintained separately (and possibly drifted)? How are new items / price changes maintained?
- **D-4 — Hunt-loot valuation rule:** the sheet estimates hunt-deposit value (col 6) but code gives
  `JAGDBEUTEN` storage value 0. What's the intended rule? (gates E3; `Category.storage` was removed as
  dead — see Decisions **D-5** — so the hunt-loot estimate is the remaining, separate question.)
- **D-6 — Output preference:** keep server-rendered HTML, or move to a JSON API + small frontend for
  the dashboard? (shapes E5)
- ~~**D-7 — Deployment target**~~ ✅ DECIDED 2026-06-15: built as a **Docker image locally** and run on the
  author's **home server** ("im Keller"); **no CI/CD for now** (optionally a *local* build gate later, low prio).
  → **A4 deprioritized**; the deploy-relevant items are **C3** (inject credentials, don't bake `zugang.txt` into
  the image) and **H3** (Dockerfile de-hack).
- ~~**D-8 — Language**~~ ✅ DECIDED 2026-06-13: conversation German; code/comments/docs English with German domain terms.
- **D-9 — BDD tooling:** plain JUnit given/when/then, or a framework (Cucumber)? Assumed plain unless asked.
- ~~**D-10 — Formatter engine (gates A7)**~~ ✅ RESOLVED 2026-06-19: **Eclipse JDT** via a single central
  `config/eclipse/formatter.xml` (tabs, `lineSplit=180`), shared by VS Code, Eclipse, IntelliJ (adapter)
  and the Gradle build (Spotless). Full **reflow accepted** for one uniform, enforced standard; VS Code
  formats + organizes imports on save; **star imports forbidden** (`starThreshold: 999999`). `.editorconfig`
  was dropped (VS-Code-only / Java-primary). See the 2026-06-19 Decision row. Wildcards have since been
  **expanded to explicit** (wildcard-free; `removeUnusedImports` added). **Formatter fine-tuning done
  2026-06-19:** only two real deviations added — chain-wrap one-per-`.` (`alignment_for_selector_in_method_invocation=48`)
  and no trailing-method-body blank (`number_of_blank_lines_at_end_of_method_body=0`). `if`-one-liner wrapping
  and multi-blank collapse turned out to be **JDT defaults**; removing the blank after `class {` / at method-body
  start is not JDT-doable without losing inter-method blanks. **Always-braces** (the formatter can't insert `{ }`)
  is enforced separately by a Checkstyle `NeedBraces` gate.
- **D-11 — ToS / PII (enterprise-audit Pitfall #7):** the tool scrapes evergore.de and stores *other*
  guild members' bank/storage activity. No production pressure (Q2), but worth a deliberate stance:
  is the scraping within the site's ToS, and is storing other members' activity acceptable for the
  guild's use? Low priority; logged so it isn't silently ignored.

## Assumptions currently baked into the plan (challenge if wrong)

1. This is a single-guild, low-traffic tool; correctness & maintainability ≫ performance.
2. The author wants to *continue* the rebuild, not abandon it.
3. The verified Gildenmehrwert formula (deposits − withdrawals across bank+storage) is the
   intended definition of member contribution.
